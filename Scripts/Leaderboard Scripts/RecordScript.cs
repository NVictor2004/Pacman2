using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class RecordScript : MonoBehaviour {
  public TextMeshProUGUI Position;    // Stores the Position game object
  public TextMeshProUGUI Name;        // Stores the Name game object
  public TextMeshProUGUI Score;       // Stores the Score game object
  public new RectTransform transform; // Stores the Record Template prefab’s RectTransform component
}