using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class TransferVariables : MonoBehaviour {
  public static int DiamondNumber = 3;   // Stores the number of diamonds in the maze
  public static int EnergizerNumber = 1; // Stores the number of energizers in the maze
  public static int GhostDelay = 1;      // Stores the number of seconds between new ghosts spawning into the maze
  public static string Username = "A";   // Stores the current user's username
  public static int Score;               // Stores the current user's score
}